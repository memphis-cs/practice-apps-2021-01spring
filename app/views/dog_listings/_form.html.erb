<%= form_with(model: dog_listing) do |form| %>
  <% if dog_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dog_listing.errors.count, "error") %> prohibited this dog_listing from being saved:</h2>

      <ul>
        <% dog_listing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :breed %>
    <%= form.text_field :breed %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :seller_email %>
    <%= form.text_field :seller_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
